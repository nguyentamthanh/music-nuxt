'use strict';

const utils = require('../chunks/utils.cjs');
require('debug');

function WindicssCss(source) {
  if (!this._compiler)
    return source;
  this.cacheable(true);
  const service = this._compiler.$windi;
  if (!service)
    return source;
  return utils.transformCSS(service, source, this.resource);
}

module.exports = WindicssCss;
